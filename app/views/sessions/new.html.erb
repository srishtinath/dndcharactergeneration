<h1>Login</h1>

<%= form_for @user do |f| %>
<%= f.label :name, "Enter username" %>
<%= f.text_field :name %>

<%= f.label :password, "Enter password" %>
<%= f.password_field :password %>
<%= f.submit("Login") %>
<% end %>

<%# Need to figure out how to not create a new one %>